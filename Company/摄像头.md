# 摄像头注意事项

## 释放资源
```java
//释放资源,否则下次调用会造成RuntiemtException：Fail to connect to camera service
 if (mCamera != null) {
     mCamera.setPreviewCallback(null);
     mCamera.stopPreview();
     mCamera.release();
     mCamera = null;
}
```
